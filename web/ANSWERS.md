1. What happened when you refreshed the browser?
    - The website updated with the changes I made.

2. What happens when you click the **About** link?
    - The `about.html` page opens.

3. What happens when you click the **Home** link?
    - The `index.html` page opens.

4. What does the 200 status code indicate?
    - 200 OK: The request succeeded.

5. What HTTP method appears in the server log?
    - The GET method.
    
6. What file is requested when you open the root URL?
    - favicon.ico is the file requested upon entering the root URL.